  {} {} {} {}
{}[] [] [] []{}
{}[] [] [] []{}
{][] [] [] []{}
{}[] [] [] []{}
  {} {} {} {}

Teniendo esta matriz, vamos a hacer comparaciones con los valores dados por argumento.

Comprobaremos que el input sea correcto (ver logic.txt para casuisticas varias)

Una vez tenemos parseado el input, pasaremos a la assignacion de cada valor.

El algoritmo que hemos pensado, se desarrollara de la siguiente forma:
Backtracking inizializado a todo 1, y por cada possibilidad passara las siguientes condiciones:

1. La suma de cada columna y cada fila de forma independiente tiene que ser = 10
2. Los 4 digitos de cada columna y cada fila tienen que ser diferentes
3. Seguiremos la logica de nuestro algoritmo magico inventado en python, que hace lo siguiente:

  {x} {x} {x} {x}
{}[] [] [] []{}
{}[] [] [] []{}
{][] [] [] []{}
{}[] [] [] []{}
  {} {} {} {}

Hacemos un condicional que divida el input en bloques de 4, haciendo referencia a las 4 "vistas" que requerimos.

  {x} {} {} {}
{}[y] [] [] []{}
{}[] [] [] []{}
{][] [] [] []{}
{}[] [] [] []{}
  {} {} {} {}

Una vez tenemos la informacion parseada, cogemos el primer dato y hacemos la siguiente comparacion:
	- Numero de entrada es x y sabemos que el numero de incrementos tiene que dar x-1
	- El "pivot" que usamos es el primer numero de la col/row, comprobamos con el siguiente, tenemos dos opciones:
		1. El pivot es mas pequenio que el siguiente, por lo tanto incremento ++ y modificamos el "pivot"
		2. Sino, continuamos iterando con el mismo "pivot".

Si en alguno de estos la condicion no se cumple devuelve 1(false) y entonces seguimos con la proxima iteracion del Backtracking. 
En caso de que sea la solucion deseada (pasa todos los filtros), devolveremos la solucion como valor final

Y ya estaria crack ;).
